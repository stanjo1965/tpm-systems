const menu=document.getElementById('menu'),nav=document.getElementById('nav');menu.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('on')}),{threshold:.13});document.querySelectorAll('.section-label,.intro-grid,.numbers,.section-head,.solution-card,.project-panel,.ax-title,.ax-flow,.ax-benefits').forEach(el=>{el.classList.add('reveal');observer.observe(el)});const count=document.querySelector('[data-count]');if(count){const co=new IntersectionObserver(([e])=>{if(e.isIntersecting){let n=0;const t=setInterval(()=>{count.textContent=++n;if(n>=2)clearInterval(t)},300);co.disconnect()}},{threshold:.6});co.observe(count)}
